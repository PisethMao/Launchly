version: "3.9"
services:
  launchly:
    container_name: launchly-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    environment:
      NODE_ENV: production
    restart: always
